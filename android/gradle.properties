org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Windows-only: the project lives on a different drive (E:) than the pub
# cache (C:), which crashes Kotlin's incremental compiler when it tries to
# compute a relative path between the two roots. Incremental compilation
# is a build-speed optimization only — disabling it just makes clean
# Kotlin compiles slightly slower, never affects the output.
kotlin.incremental=false
